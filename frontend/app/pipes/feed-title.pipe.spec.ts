import { FeedTitlePipe } from './feed-title.pipe';

describe('FeedTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new FeedTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
